# Frontend KOAJ Access

Este modulo corresponde a la aplicacion React + TypeScript + Vite del sistema.

La guia completa de instalacion esta en el README raiz del repositorio.

Ruta recomendada:

- ../README.md

Comandos rapidos (desarrollo):

```powershell
cd frontend
Copy-Item .env.example .env
npm ci
npm run dev
```

